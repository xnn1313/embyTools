# File: nanshare_db.py.1shot.seq (Python 3.12)
# Source generated by Pyarmor-Static-Unpack-1shot (v0.3.0), powered by Decompyle++ (pycdc)

# Note: Decompiled code can be incomplete and incorrect.
# Please also check the correct and complete disassembly file: nanshare_db.py.1shot.das

__doc__ = 'NanShare 统一数据库模块 - 将整理历史、订阅、STRM 日志三张表合并到单一 NanShare.db 中管理。'
'__pyarmor_enter_6547__(...)'
import json
import os
import sqlite3
import uuid
from datetime import datetime
from pathlib import Path
from typing import Dict, List, Optional, Tuple

class NanShareDB:
    '''统一数据库管理，包含 transfer_histories / subscribes / strm_task_logs 三张表。'''
    '__pyarmor_enter_6550__(...)'
    
    def __init__(self, db_path):
        '__pyarmor_enter_6553__(...)'
    # WARNING: Decompyle incomplete

    _resolve_db_path = (lambda : '__pyarmor_enter_6556__(...)'candidates = [
Path(__file__).parent.parent / 'config',
Path(os.path.dirname(os.path.abspath(__file__))).parent / 'config',
Path(os.getcwd()) / 'config']for config_dir in candidates:
resolved_dir = config_dir.resolve()resolved_dir.mkdir(parents = True, exist_ok = True)if resolved_dir.is_dir():
candidates, str(resolved_dir / 'NanShare.db')None(candidates)Nonestr((Path(os.getcwd()) / 'NanShare.db').resolve())None(None)None# WARNING: Decompyle incomplete
)()
    
    def _get_connection(self):
        '__pyarmor_enter_6559__(...)'
        conn = sqlite3.connect(self.db_path, timeout = 30)
        conn.row_factory = sqlite3.Row
        return conn
        None(None)
        return None
        '__pyarmor_exit_6560__(...)'
        '__pyarmor_exit_6560__(...)'

    _json_dumps = (lambda value, default: '__pyarmor_enter_6562__(...)'# WARNING: Decompyle incomplete
)()
    _json_loads = (lambda value, default: '__pyarmor_enter_6565__(...)'if value in (None, ''):
defaultNone(None)Nonejson.loads(value)None(None)None# WARNING: Decompyle incomplete
)()
    
    def _init_db(self):
        '__pyarmor_enter_6568__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        self._init_transfer_histories(cursor)
        self._init_subscribes(cursor)
        self._init_strm_task_logs(cursor)
        self._init_tg_forward_logs(cursor)
        self._init_docker_logs(cursor)
        self._init_ed2k_strm_logs(cursor)
        cursor.execute('PRAGMA journal_mode=WAL')
        conn.commit()
        conn.close()
        return None
        return None
        conn.close()
        '__pyarmor_exit_6569__(...)'
        '__pyarmor_exit_6569__(...)'

    _init_transfer_histories = (lambda cursor: '__pyarmor_enter_6571__(...)'cursor.execute("\n            CREATE TABLE IF NOT EXISTS transfer_histories (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                src TEXT,\n                src_storage TEXT DEFAULT 'local',\n                src_fileitem TEXT DEFAULT '{}',\n                dest TEXT,\n                dest_storage TEXT DEFAULT 'local',\n                dest_fileitem TEXT DEFAULT '{}',\n                mode TEXT,\n                config_id TEXT,\n                config_name TEXT,\n                type TEXT,\n                category TEXT,\n                title TEXT,\n                year TEXT,\n                tmdbid INTEGER,\n                imdbid TEXT,\n                tvdbid INTEGER,\n                doubanid TEXT,\n                seasons TEXT,\n                episodes TEXT,\n                episode_group TEXT,\n                status TEXT DEFAULT 'success',\n                result_status TEXT DEFAULT '',\n                errmsg TEXT,\n                date TEXT DEFAULT CURRENT_TIMESTAMP,\n                files TEXT DEFAULT '[]',\n                time_usage_seconds REAL DEFAULT 0,\n                result_data TEXT DEFAULT '{}'\n            )\n            ")# WARNING: Decompyle incomplete
)()
    _init_subscribes = (lambda cursor: '__pyarmor_enter_6574__(...)'cursor.execute("\n            CREATE TABLE IF NOT EXISTS subscribes (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                subscribe_id TEXT UNIQUE NOT NULL,\n                name TEXT NOT NULL,\n                year TEXT,\n                type TEXT NOT NULL DEFAULT 'movie',\n                tmdb_id INTEGER,\n                poster_path TEXT,\n                backdrop_path TEXT,\n                overview TEXT,\n                total_season INTEGER DEFAULT 0,\n                total_episode INTEGER DEFAULT 0,\n                current_season INTEGER DEFAULT 1,\n                lack_episode TEXT DEFAULT '[]',\n                resolution TEXT DEFAULT '',\n                effect TEXT DEFAULT '',\n                resource_type TEXT DEFAULT '',\n                release_group TEXT DEFAULT '',\n                mode TEXT NOT NULL DEFAULT 'share_strm',\n                transfer_use_115_config TEXT DEFAULT '',\n                transfer_folders TEXT DEFAULT '[]',\n                share_strm_folders TEXT DEFAULT '[]',\n                parse_mode INTEGER DEFAULT 3,\n                ffprobe_enabled INTEGER DEFAULT 0,\n                reshare_enabled INTEGER DEFAULT 0,\n                transfer_all_matches INTEGER DEFAULT 0,\n                original_title TEXT DEFAULT '',\n                streaming_platform TEXT DEFAULT '',\n                strm_folder_path TEXT DEFAULT '',\n                start_episode INTEGER DEFAULT 1,\n                status TEXT DEFAULT 'active',\n                search_source TEXT DEFAULT 'tg_hdhive',\n                cron_expression TEXT DEFAULT '0 */2 * * *',\n                last_run_time TEXT,\n                created_at TEXT DEFAULT CURRENT_TIMESTAMP,\n                updated_at TEXT DEFAULT CURRENT_TIMESTAMP\n            )\n        ")cursor.execute('CREATE INDEX IF NOT EXISTS idx_sub_status ON subscribes(status)')cursor.execute('CREATE INDEX IF NOT EXISTS idx_sub_type ON subscribes(type)')cursor.execute('CREATE INDEX IF NOT EXISTS idx_sub_tmdb ON subscribes(tmdb_id)')# WARNING: Decompyle incomplete
)()
    _init_strm_task_logs = (lambda cursor: '__pyarmor_enter_6577__(...)'cursor.execute("\n            CREATE TABLE IF NOT EXISTS strm_task_logs (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                task_id TEXT UNIQUE NOT NULL,\n                share_url TEXT NOT NULL,\n                share_code TEXT,\n                status TEXT DEFAULT 'pending',\n                is_reshare INTEGER DEFAULT 0,\n                strm_success INTEGER DEFAULT 0,\n                strm_fail INTEGER DEFAULT 0,\n                metadata_success INTEGER DEFAULT 0,\n                metadata_fail INTEGER DEFAULT 0,\n                total_files INTEGER DEFAULT 0,\n                error_message TEXT,\n                new_share_url TEXT,\n                local_path TEXT,\n                speed_mode INTEGER DEFAULT 0,\n                elapsed_time REAL DEFAULT 0,\n                source TEXT DEFAULT '',\n                detail_log TEXT DEFAULT '[]',\n                created_at TEXT DEFAULT CURRENT_TIMESTAMP,\n                updated_at TEXT DEFAULT CURRENT_TIMESTAMP\n            )\n        ")cursor.execute('ALTER TABLE strm_task_logs ADD COLUMN new_share_url TEXT')cursor.execute("ALTER TABLE strm_task_logs ADD COLUMN source TEXT DEFAULT ''")cursor.execute('CREATE INDEX IF NOT EXISTS idx_strm_status ON strm_task_logs(status)')cursor.execute('CREATE INDEX IF NOT EXISTS idx_strm_is_reshare ON strm_task_logs(is_reshare)')cursor.execute('CREATE INDEX IF NOT EXISTS idx_strm_created_at ON strm_task_logs(created_at)')NoneNone# WARNING: Decompyle incomplete
)()
    _init_tg_forward_logs = (lambda cursor: '__pyarmor_enter_6580__(...)'cursor.execute("\n            CREATE TABLE IF NOT EXISTS tg_forward_logs (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                forward_config_id TEXT,\n                forward_config_name TEXT,\n                tg_config_id TEXT,\n                tg_config_name TEXT,\n                source_id TEXT,\n                source_name TEXT,\n                target_id TEXT,\n                target_name TEXT,\n                message_id INTEGER,\n                message_text TEXT,\n                message_type TEXT DEFAULT '',\n                message_link TEXT DEFAULT '',\n                matched_keyword TEXT DEFAULT '',\n                super_forward INTEGER DEFAULT 0,\n                status TEXT DEFAULT 'success',\n                error_message TEXT DEFAULT '',\n                created_at TEXT DEFAULT CURRENT_TIMESTAMP\n            )\n        ")# WARNING: Decompyle incomplete
)()
    
    def add_tg_forward_log(self, data):
        '__pyarmor_enter_6583__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('\n                INSERT INTO tg_forward_logs\n                (forward_config_id, forward_config_name, tg_config_id, tg_config_name,\n                 source_id, source_name, target_id, target_name,\n                 message_id, message_text, message_type, message_link,\n                 matched_keyword, super_forward, status, error_message, created_at)\n                VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n            ', (data.get('forward_config_id', ''), data.get('forward_config_name', ''), data.get('tg_config_id', ''), data.get('tg_config_name', ''), data.get('source_id', ''), data.get('source_name', ''), data.get('target_id', ''), data.get('target_name', ''), data.get('message_id'), data.get('message_text', ''), data.get('message_type', ''), data.get('message_link', ''), data.get('matched_keyword', ''), 1 if data.get('super_forward') else 0, data.get('status', 'success'), data.get('error_message', ''), data.get('created_at', datetime.now().strftime('%Y-%m-%d %H:%M:%S'))))
        conn.commit()
        conn.close()
        return cursor.lastrowid
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6584__(...)'
        '__pyarmor_exit_6584__(...)'

    
    def get_tg_forward_logs(self, page, page_size, forward_config_id, tg_config_id, source_id, target_id, status, keyword):
        '__pyarmor_enter_6586__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        conditions = []
        params = []
        if forward_config_id:
            conditions.append('forward_config_id = ?')
            params.append(forward_config_id)
        if tg_config_id:
            conditions.append('tg_config_id = ?')
            params.append(tg_config_id)
        if source_id:
            conditions.append('source_id = ?')
            params.append(source_id)
        if target_id:
            conditions.append('target_id = ?')
            params.append(target_id)
        if status:
            conditions.append('status = ?')
            params.append(status)
        if keyword:
            conditions.append('(message_text LIKE ? OR source_name LIKE ? OR target_name LIKE ? OR forward_config_name LIKE ? OR matched_keyword LIKE ?)')
            like_val = f'''%{keyword}%'''
            params.extend([
                like_val,
                like_val,
                like_val,
                like_val,
                like_val])
        where_clause = ' AND '.join(conditions) if conditions else '1=1'
        cursor.execute(f'''SELECT COUNT(*) FROM tg_forward_logs WHERE {where_clause}''', params)
        total = cursor.fetchone()[0]
        offset = (page - 1) * page_size
        cursor.execute(f'''\n                SELECT * FROM tg_forward_logs WHERE {where_clause}\n                ORDER BY created_at DESC, id DESC LIMIT ? OFFSET ?\n            ''', params + [
            page_size,
            offset])
        rows = cursor.fetchall()
    # WARNING: Decompyle incomplete

    
    def get_tg_forward_log(self, log_id):
        '__pyarmor_enter_6589__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM tg_forward_logs WHERE id = ?', (log_id,))
        row = cursor.fetchone()
        conn.close()
        return self._tg_fwd_log_row_to_dict(row) if row else None
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6590__(...)'
        '__pyarmor_exit_6590__(...)'

    
    def delete_tg_forward_log(self, log_id):
        '__pyarmor_enter_6592__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM tg_forward_logs WHERE id = ?', (log_id,))
        conn.commit()
        conn.close()
        return cursor.rowcount > 0
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6593__(...)'
        '__pyarmor_exit_6593__(...)'

    
    def delete_tg_forward_logs(self, log_ids):
        '__pyarmor_enter_6595__(...)'
        if not log_ids:
            return 0
        return 0
        normalized = []
        for lid in log_ids:
            normalized.append(int(lid))
        if not normalized:
            return 0
        return 0
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def delete_all_tg_forward_logs(self):
        '__pyarmor_enter_6598__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM tg_forward_logs')
        conn.commit()
        conn.close()
        return cursor.rowcount
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6599__(...)'
        '__pyarmor_exit_6599__(...)'

    
    def get_tg_forward_log_filter_options(self):
        '__pyarmor_enter_6601__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT DISTINCT source_id, source_name FROM tg_forward_logs WHERE source_id IS NOT NULL AND source_id != ""')
    # WARNING: Decompyle incomplete

    _tg_fwd_log_row_to_dict = (lambda row: '__pyarmor_enter_6604__(...)'# WARNING: Decompyle incomplete
)()
    
    def get_history(self, history_id):
        '__pyarmor_enter_6607__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM transfer_histories WHERE id = ?', (history_id,))
        row = cursor.fetchone()
        conn.close()
        return self._history_row_to_dict(row) if row else None
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6608__(...)'
        '__pyarmor_exit_6608__(...)'

    
    def get_histories(self, page, page_size, status, keyword, history_ids, ids_only):
        '__pyarmor_enter_6610__(...)'
        if not page:
            page
        page = max(int(1), 1)
        if not page_size:
            page_size
        page_size = int(0)
        conn = self._get_connection()
        cursor = conn.cursor()
        conditions = []
        params = []
        if not status:
            status
        normalized_status = str('').strip().lower()
        if normalized_status in frozenset({'failed', 'success'}):
            conditions.append('status = ?')
            params.append(normalized_status)
        if not keyword:
            keyword
        normalized_keyword = str('').strip()
        if normalized_keyword:
            conditions.append('(title LIKE ? OR src LIKE ? OR dest LIKE ?)')
            like_value = f'''%{normalized_keyword}%'''
            params.extend([
                like_value,
                like_value,
                like_value])
        normalized_ids = []
        if not history_ids:
            history_ids
        for history_id in []:
            normalized_ids.append(int(history_id))
    # WARNING: Decompyle incomplete

    
    def add_force(self, data, replace_history_id):
        '__pyarmor_enter_6613__(...)'
        if not data:
            data
        payload = dict({ })
        if not payload.get('src'):
            payload.get('src')
        src = str('').strip()
        conn = self._get_connection()
        cursor = conn.cursor()
        if replace_history_id:
            cursor.execute('DELETE FROM transfer_histories WHERE id = ?', (int(replace_history_id),))
        if src:
            cursor.execute('DELETE FROM transfer_histories WHERE src = ?', (src,))
        payload.setdefault('date', datetime.now().strftime('%Y-%m-%d %H:%M:%S'))
        if not payload.get('src_storage'):
            payload.get('src_storage')
        payload['src_storage'] = str('local')
        payload['src_fileitem'] = self._json_dumps(payload.get('src_fileitem'), { })
        if not payload.get('dest_storage'):
            payload.get('dest_storage')
        payload['dest_storage'] = str('local')
        payload['dest_fileitem'] = self._json_dumps(payload.get('dest_fileitem'), { })
        payload['files'] = self._json_dumps(payload.get('files'), [])
        payload['result_data'] = self._json_dumps(payload.get('result_data'), { })
        columns = [
            'src',
            'src_storage',
            'src_fileitem',
            'dest',
            'dest_storage',
            'dest_fileitem',
            'mode',
            'config_id',
            'config_name',
            'type',
            'category',
            'title',
            'year',
            'tmdbid',
            'imdbid',
            'tvdbid',
            'doubanid',
            'seasons',
            'episodes',
            'episode_group',
            'status',
            'result_status',
            'errmsg',
            'date',
            'files',
            'result_data']
    # WARNING: Decompyle incomplete

    
    def delete_history(self, history_id):
        '__pyarmor_enter_6616__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM transfer_histories WHERE id = ?', (history_id,))
        conn.commit()
        conn.close()
        return cursor.rowcount > 0
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6617__(...)'
        '__pyarmor_exit_6617__(...)'

    
    def delete_histories(self, history_ids):
        '__pyarmor_enter_6619__(...)'
        normalized_ids = []
        if not history_ids:
            history_ids
        for history_id in []:
            normalized_ids.append(int(history_id))
        if not normalized_ids:
            return 0
        return 0
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def delete_all_histories(self):
        '__pyarmor_enter_6622__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM transfer_histories')
        conn.commit()
        conn.close()
        return cursor.rowcount
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6623__(...)'
        '__pyarmor_exit_6623__(...)'

    
    def _history_row_to_dict(self, row):
        '__pyarmor_enter_6625__(...)'
    # WARNING: Decompyle incomplete

    
    def add_subscribe(self, data):
        '__pyarmor_enter_6628__(...)'
        subscribe_id = str(uuid.uuid4())[:8]
        conn = self._get_connection()
        cursor = conn.cursor()
        [][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')][data.get('strm_folder_path', '')][data.get('start_episode', 1)][data.get('status', 'active')][data.get('search_source', 'tg_hdhive')]([][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')][data.get('strm_folder_path', '')][data.get('start_episode', 1)][data.get('status', 'active')][data.get('search_source', 'tg_hdhive')][data.get('cron_expression', '0 */2 * * *')], [][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')][data.get('strm_folder_path', '')][data.get('start_episode', 1)][data.get('status', 'active')][data.get('search_source', 'tg_hdhive')][data.get('cron_expression', '0 */2 * * *')][json.dumps(data.get('preset_ids', []), ensure_ascii = False)])
        conn.commit()
        conn.close()
        return subscribe_id
        [][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')][data.get('strm_folder_path', '')][data.get('start_episode', 1)]([][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')][data.get('strm_folder_path', '')][data.get('start_episode', 1)][data.get('status', 'active')])
        return [][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')][data.get('strm_folder_path', '')]
        conn.close()
        '__pyarmor_exit_6629__(...)'
        '__pyarmor_exit_6629__(...)'
        return [][subscribe_id][data.get('name', '')][data.get('year')][data.get('type', 'movie')][data.get('tmdb_id')][data.get('poster_path', '')][data.get('backdrop_path', '')][data.get('overview', '')][data.get('total_season', 0)][data.get('total_episode', 0)][data.get('current_season', 1)][json.dumps(data.get('lack_episode', []), ensure_ascii = False)][data.get('resolution', '')][data.get('effect', '')][data.get('resource_type', '')][data.get('release_group', '')][data.get('frame_rate', '')][data.get('mode', 'share_strm')][data.get('transfer_use_115_config', '')][json.dumps(data.get('transfer_folders', []), ensure_ascii = False)][json.dumps(data.get('share_strm_folders', []), ensure_ascii = False)][data.get('parse_mode')][1 if data.get('ffprobe_enabled') else 0][1 if data.get('reshare_enabled') else 0][1 if data.get('transfer_all_matches') else 0][data.get('original_title', '')][data.get('streaming_platform', '')]

    _UPDATABLE_COLUMNS = {
        'mode',
        'name',
        'type',
        'year',
        'effect',
        'status',
        'tmdb_id',
        'overview',
        'frame_rate',
        'parse_mode',
        'preset_ids',
        'resolution',
        'poster_path',
        'lack_episode',
        'total_season',
        'backdrop_path',
        'last_run_time',
        'release_group',
        'resource_type',
        'search_source',
        'start_episode',
        'total_episode',
        'current_season',
        'original_title',
        'cron_expression',
        'ffprobe_enabled',
        'reshare_enabled',
        'strm_folder_path',
        'transfer_folders',
        'share_strm_folders',
        'streaming_platform',
        'transfer_all_matches',
        'transfer_use_115_config'}
    
    def update_subscribe(self, subscribe_id, data):
        '__pyarmor_enter_6631__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        fields = []
        values = []
        for key, value in data.items():
            if key not in self._UPDATABLE_COLUMNS:
                continue
            if key in ('lack_episode', 'transfer_folders', 'share_strm_folders', 'preset_ids'):
                value = json.dumps(value, ensure_ascii = False) if isinstance(value, (list, dict)) else value
            if key in ('ffprobe_enabled', 'reshare_enabled', 'transfer_all_matches'):
                value = 1 if value else 0
            fields.append(f'''{key} = ?''')
            values.append(value)
        fields.append('updated_at = ?')
        values.append(datetime.now().isoformat())
        values.append(subscribe_id)
        cursor.execute(f'''\n                UPDATE subscribes SET {', '.join(fields)} WHERE subscribe_id = ?\n            ''', values)
        conn.commit()
        conn.close()
        return cursor.rowcount > 0
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6632__(...)'
        '__pyarmor_exit_6632__(...)'

    
    def delete_subscribe(self, subscribe_id):
        '__pyarmor_enter_6634__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM subscribes WHERE subscribe_id = ?', (subscribe_id,))
        conn.commit()
        conn.close()
        return cursor.rowcount > 0
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6635__(...)'
        '__pyarmor_exit_6635__(...)'

    
    def delete_subscribes(self, subscribe_ids):
        '__pyarmor_enter_6637__(...)'
        if not subscribe_ids:
            return 0
        return 0
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def delete_all_subscribes(self, mode):
        '__pyarmor_enter_6640__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        if mode:
            cursor.execute('DELETE FROM subscribes WHERE mode = ?', (mode,))
        else:
            cursor.execute('DELETE FROM subscribes')
        conn.commit()
        conn.close()
        return cursor.rowcount
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6641__(...)'
        '__pyarmor_exit_6641__(...)'

    
    def get_subscribe(self, subscribe_id):
        '__pyarmor_enter_6643__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM subscribes WHERE subscribe_id = ?', (subscribe_id,))
        row = cursor.fetchone()
        if row:
            conn.close()
            return self._sub_row_to_dict(row)
        None(None)
        return None
        conn.close()
        return None
        return None
        conn.close()
        '__pyarmor_exit_6644__(...)'
        '__pyarmor_exit_6644__(...)'

    
    def get_subscribe_by_tmdb(self, tmdb_id, media_type, current_season):
        '__pyarmor_enter_6646__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def get_subscribes(self, page, page_size, status, media_type, mode, keyword):
        '__pyarmor_enter_6649__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        conditions = []
        params = []
        if status:
            conditions.append('status = ?')
            params.append(status)
        if media_type:
            conditions.append('type = ?')
            params.append(media_type)
        if mode:
            conditions.append('mode = ?')
            params.append(mode)
        if keyword:
            conditions.append('(name LIKE ?)')
            params.append(f'''%{keyword}%''')
        where_clause = ' AND '.join(conditions) if conditions else '1=1'
        cursor.execute(f'''SELECT COUNT(*) FROM subscribes WHERE {where_clause}''', params)
        total = cursor.fetchone()[0]
        offset = (page - 1) * page_size
        cursor.execute(f'''\n                SELECT * FROM subscribes WHERE {where_clause}\n                ORDER BY created_at DESC LIMIT ? OFFSET ?\n            ''', params + [
            page_size,
            offset])
        rows = cursor.fetchall()
    # WARNING: Decompyle incomplete

    
    def get_subscribed_tmdb_ids(self):
        '__pyarmor_enter_6652__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT DISTINCT tmdb_id, type FROM subscribes WHERE tmdb_id IS NOT NULL AND tmdb_id > 0')
    # WARNING: Decompyle incomplete

    
    def get_active_subscribes(self, mode):
        '__pyarmor_enter_6655__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        if mode:
            cursor.execute('SELECT * FROM subscribes WHERE status = ? AND mode = ? ORDER BY created_at ASC', ('active', mode))
        else:
            cursor.execute('SELECT * FROM subscribes WHERE status = ? ORDER BY created_at ASC', ('active',))
        rows = cursor.fetchall()
    # WARNING: Decompyle incomplete

    
    def _sub_row_to_dict(self, row):
        '__pyarmor_enter_6658__(...)'
    # WARNING: Decompyle incomplete

    _format_episode_range_static = (lambda eps: '__pyarmor_enter_6661__(...)'if not eps:
''''sorted_eps = sorted(eps)ranges = []start = sorted_eps[0]end = sorted_eps[0]for e in sorted_eps[1:]:
if e == end + 1:
end = econtinueranges.append((start, end))start = eend = eranges.append((start, end))parts = []for s, e in ranges:
if s == e:
parts.append(f'''E{s:02d}''')continueparts.append(f'''E{s:02d}-{e:02d}''')' '.join(parts)None(None)None'__pyarmor_exit_6662__(...)''__pyarmor_exit_6662__(...)')()
    
    def create_task(self, share_url, share_code, is_reshare, local_path, speed_mode, source):
        '__pyarmor_enter_6664__(...)'
        task_id = str(uuid.uuid4())
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute("\n                INSERT INTO strm_task_logs \n                (task_id, share_url, share_code, status, is_reshare, local_path, speed_mode, source, detail_log)\n                VALUES (?, ?, ?, 'running', ?, ?, ?, ?, '[]')\n            ", (task_id, share_url, share_code, 1 if is_reshare else 0, local_path, speed_mode, source))
        conn.commit()
        conn.close()
        return task_id
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6665__(...)'
        '__pyarmor_exit_6665__(...)'

    
    def update_task_status(self, task_id, status, error_message):
        '__pyarmor_enter_6667__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('\n                UPDATE strm_task_logs \n                SET status = ?, error_message = ?, updated_at = ?\n                WHERE task_id = ?\n            ', (status, error_message, datetime.now().isoformat(), task_id))
        conn.commit()
        conn.close()
        return None
        return None
        conn.close()
        '__pyarmor_exit_6668__(...)'
        '__pyarmor_exit_6668__(...)'

    
    def update_task_result(self, task_id, status, strm_success, strm_fail, metadata_success, metadata_fail, total_files, elapsed_time, error_message, new_share_url):
        '__pyarmor_enter_6670__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('\n                UPDATE strm_task_logs \n                SET status = ?, strm_success = ?, strm_fail = ?, \n                    metadata_success = ?, metadata_fail = ?, total_files = ?,\n                    elapsed_time = ?, error_message = ?, new_share_url = ?, updated_at = ?\n                WHERE task_id = ?\n            ', (status, strm_success, strm_fail, metadata_success, metadata_fail, total_files, elapsed_time, error_message, new_share_url, datetime.now().isoformat(), task_id))
        conn.commit()
        conn.close()
        return None
        return None
        conn.close()
        '__pyarmor_exit_6671__(...)'
        '__pyarmor_exit_6671__(...)'

    
    def append_detail_log(self, task_id, log_entry):
        '__pyarmor_enter_6673__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT detail_log FROM strm_task_logs WHERE task_id = ?', (task_id,))
        row = cursor.fetchone()
        if row:
            if not row['detail_log']:
                row['detail_log']
            logs = json.loads('[]')
            logs.append({
                'time': datetime.now().strftime('%H:%M:%S'),
                'message': log_entry })
            if len(logs) > 500:
                logs = logs[-500:]
            cursor.execute('\n                    UPDATE strm_task_logs SET detail_log = ?, updated_at = ?\n                    WHERE task_id = ?\n                ', (json.dumps(logs, ensure_ascii = False), datetime.now().isoformat(), task_id))
            conn.commit()
        conn.close()
        return None
        return None
    # WARNING: Decompyle incomplete

    
    def get_task(self, task_id):
        '__pyarmor_enter_6676__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM strm_task_logs WHERE task_id = ?', (task_id,))
        row = cursor.fetchone()
        if row:
            conn.close()
            return self._task_row_to_dict(row)
        None(None)
        return None
        conn.close()
        return None
        return None
        conn.close()
        '__pyarmor_exit_6677__(...)'
        '__pyarmor_exit_6677__(...)'

    
    def get_tasks(self, page, page_size, status, is_reshare, keyword, source):
        '__pyarmor_enter_6679__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        conditions = []
        params = []
        if status:
            conditions.append('status = ?')
            params.append(status)
    # WARNING: Decompyle incomplete

    
    def delete_task(self, task_id):
        '__pyarmor_enter_6682__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM strm_task_logs WHERE task_id = ?', (task_id,))
        conn.commit()
        conn.close()
        return cursor.rowcount > 0
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6683__(...)'
        '__pyarmor_exit_6683__(...)'

    
    def delete_tasks(self, task_ids):
        '__pyarmor_enter_6685__(...)'
        if not task_ids:
            return 0
        return 0
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def delete_all_tasks(self):
        '__pyarmor_enter_6688__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM strm_task_logs')
        conn.commit()
        conn.close()
        return cursor.rowcount
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6689__(...)'
        '__pyarmor_exit_6689__(...)'

    
    def _task_row_to_dict(self, row, include_detail):
        '__pyarmor_enter_6691__(...)'
    # WARNING: Decompyle incomplete

    _init_docker_logs = (lambda cursor: '__pyarmor_enter_6694__(...)'cursor.execute("\n            CREATE TABLE IF NOT EXISTS docker_logs (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                operation TEXT NOT NULL,\n                target_type TEXT DEFAULT 'container',\n                target_name TEXT DEFAULT '',\n                target_id TEXT DEFAULT '',\n                image TEXT DEFAULT '',\n                status TEXT DEFAULT 'success',\n                message TEXT DEFAULT '',\n                detail_log TEXT DEFAULT '[]',\n                created_at TEXT DEFAULT CURRENT_TIMESTAMP\n            )\n        ")# WARNING: Decompyle incomplete
)()
    
    def add_docker_log(self, data):
        '__pyarmor_enter_6697__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        detail_log = data.get('detail_log', [])
        if isinstance(detail_log, list):
            detail_log = json.dumps(detail_log, ensure_ascii = False)
        cursor.execute('\n                INSERT INTO docker_logs\n                (operation, target_type, target_name, target_id, image, status, message, detail_log, created_at)\n                VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)\n            ', (data.get('operation', ''), data.get('target_type', 'container'), data.get('target_name', ''), data.get('target_id', ''), data.get('image', ''), data.get('status', 'success'), data.get('message', ''), detail_log, data.get('created_at', datetime.now().strftime('%Y-%m-%d %H:%M:%S'))))
        conn.commit()
        conn.close()
        return cursor.lastrowid
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6698__(...)'
        '__pyarmor_exit_6698__(...)'

    
    def get_docker_logs(self, page, page_size, status, operation, keyword):
        '__pyarmor_enter_6700__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        conditions = []
        params = []
        if status:
            conditions.append('status = ?')
            params.append(status)
        if operation:
            conditions.append('operation = ?')
            params.append(operation)
        if keyword:
            conditions.append('(target_name LIKE ? OR image LIKE ? OR message LIKE ?)')
            like_val = f'''%{keyword}%'''
            params.extend([
                like_val,
                like_val,
                like_val])
        where_clause = ' AND '.join(conditions) if conditions else '1=1'
        cursor.execute(f'''SELECT COUNT(*) FROM docker_logs WHERE {where_clause}''', params)
        total = cursor.fetchone()[0]
        offset = (page - 1) * page_size
        cursor.execute(f'''\n                SELECT * FROM docker_logs WHERE {where_clause}\n                ORDER BY created_at DESC, id DESC LIMIT ? OFFSET ?\n            ''', params + [
            page_size,
            offset])
        rows = cursor.fetchall()
    # WARNING: Decompyle incomplete

    
    def get_docker_log(self, log_id):
        '__pyarmor_enter_6703__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM docker_logs WHERE id = ?', (log_id,))
        row = cursor.fetchone()
        conn.close()
        return self._docker_log_row_to_dict(row) if row else None
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6704__(...)'
        '__pyarmor_exit_6704__(...)'

    
    def delete_docker_logs(self, log_ids):
        '__pyarmor_enter_6706__(...)'
        if not log_ids:
            return 0
        return 0
        normalized = []
        for lid in log_ids:
            normalized.append(int(lid))
        if not normalized:
            return 0
        return 0
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def delete_all_docker_logs(self):
        '__pyarmor_enter_6709__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM docker_logs')
        conn.commit()
        conn.close()
        return cursor.rowcount
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6710__(...)'
        '__pyarmor_exit_6710__(...)'

    
    def _docker_log_row_to_dict(self, row):
        '__pyarmor_enter_6712__(...)'
        result = {
            'id': row['id'],
            'operation': row['operation'],
            'target_type': row['target_type'],
            'target_name': row['target_name'],
            'target_id': row['target_id'],
            'image': row['image'],
            'status': row['status'],
            'message': row['message'],
            'detail_log': self._json_loads(row['detail_log'], []),
            'created_at': row['created_at'] }
        return result
        None(None)
        return None
        '__pyarmor_exit_6713__(...)'
        '__pyarmor_exit_6713__(...)'

    _init_ed2k_strm_logs = (lambda cursor: '__pyarmor_enter_6715__(...)'cursor.execute("\n            CREATE TABLE IF NOT EXISTS ed2k_strm_logs (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                source TEXT DEFAULT 'tgbot',\n                status TEXT DEFAULT 'success',\n                output_dir TEXT DEFAULT '',\n                total_links INTEGER DEFAULT 0,\n                generated INTEGER DEFAULT 0,\n                filtered INTEGER DEFAULT 0,\n                failed INTEGER DEFAULT 0,\n                detail_log TEXT DEFAULT '[]',\n                error_message TEXT DEFAULT '',\n                created_at TEXT DEFAULT CURRENT_TIMESTAMP\n            )\n        ")# WARNING: Decompyle incomplete
)()
    
    def add_ed2k_strm_log(self, data):
        '__pyarmor_enter_6718__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        detail_log = data.get('detail_log', [])
        if isinstance(detail_log, list):
            detail_log = json.dumps(detail_log, ensure_ascii = False)
        cursor.execute('\n                INSERT INTO ed2k_strm_logs\n                (source, status, output_dir, total_links, generated, filtered, failed, detail_log, error_message, created_at)\n                VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n            ', (data.get('source', 'tgbot'), data.get('status', 'success'), data.get('output_dir', ''), data.get('total_links', 0), data.get('generated', 0), data.get('filtered', 0), data.get('failed', 0), detail_log, data.get('error_message', ''), data.get('created_at', datetime.now().strftime('%Y-%m-%d %H:%M:%S'))))
        conn.commit()
        conn.close()
        return cursor.lastrowid
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6719__(...)'
        '__pyarmor_exit_6719__(...)'

    
    def get_ed2k_strm_logs(self, page, page_size, status, keyword):
        '__pyarmor_enter_6721__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        conditions = []
        params = []
        if status:
            conditions.append('status = ?')
            params.append(status)
        if keyword:
            conditions.append('(output_dir LIKE ? OR error_message LIKE ? OR detail_log LIKE ?)')
            like_val = f'''%{keyword}%'''
            params.extend([
                like_val,
                like_val,
                like_val])
        where_clause = ' AND '.join(conditions) if conditions else '1=1'
        cursor.execute(f'''SELECT COUNT(*) FROM ed2k_strm_logs WHERE {where_clause}''', params)
        total = cursor.fetchone()[0]
        offset = (page - 1) * page_size
        cursor.execute(f'''\n                SELECT * FROM ed2k_strm_logs WHERE {where_clause}\n                ORDER BY created_at DESC, id DESC LIMIT ? OFFSET ?\n            ''', params + [
            page_size,
            offset])
        rows = cursor.fetchall()
    # WARNING: Decompyle incomplete

    
    def get_ed2k_strm_log(self, log_id):
        '__pyarmor_enter_6724__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT * FROM ed2k_strm_logs WHERE id = ?', (log_id,))
        row = cursor.fetchone()
        conn.close()
        return self._ed2k_strm_log_row_to_dict(row, include_detail = True) if row else None
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6725__(...)'
        '__pyarmor_exit_6725__(...)'

    
    def delete_ed2k_strm_logs(self, log_ids):
        '__pyarmor_enter_6727__(...)'
        if not log_ids:
            return 0
        return 0
        normalized = []
        for lid in log_ids:
            normalized.append(int(lid))
        if not normalized:
            return 0
        return 0
        conn = self._get_connection()
        cursor = conn.cursor()
    # WARNING: Decompyle incomplete

    
    def delete_all_ed2k_strm_logs(self):
        '__pyarmor_enter_6730__(...)'
        conn = self._get_connection()
        cursor = conn.cursor()
        cursor.execute('DELETE FROM ed2k_strm_logs')
        conn.commit()
        conn.close()
        return cursor.rowcount
        None(None)
        return None
        conn.close()
        '__pyarmor_exit_6731__(...)'
        '__pyarmor_exit_6731__(...)'

    
    def _ed2k_strm_log_row_to_dict(self, row, include_detail):
        '__pyarmor_enter_6733__(...)'
        result = {
            'id': row['id'],
            'source': row['source'],
            'status': row['status'],
            'output_dir': row['output_dir'],
            'total_links': row['total_links'],
            'generated': row['generated'],
            'filtered': row['filtered'],
            'failed': row['failed'],
            'error_message': row['error_message'],
            'created_at': row['created_at'] }
        if include_detail:
            result['detail_log'] = self._json_loads(row['detail_log'], [])
        return result
        None(None)
        return None
        '__pyarmor_exit_6734__(...)'
        '__pyarmor_exit_6734__(...)'

    return None
    staticmethod
    return None
    '__pyarmor_exit_6551__(...)'
    '__pyarmor_exit_6551__(...)'
    return staticmethod

nanshare_db = NanShareDB()
transfer_history_db = nanshare_db
subscribe_db = nanshare_db
share_strm_log_db = nanshare_db
ed2k_strm_log_db = nanshare_db
return None
return None
# WARNING: Decompyle incomplete
