# File: account_delay.py.1shot.seq (Python 3.12)
# Source generated by Pyarmor-Static-Unpack-1shot (v0.3.0), powered by Decompyle++ (pycdc)

# Note: Decompiled code can be incomplete and incorrect.
# Please also check the correct and complete disassembly file: account_delay.py.1shot.das

'''以账号为基准的随机延迟管理器

每个网盘账号（源账号、目标账号分别独立）维护一个上次 API 调用时间戳。
调用 wait() 时，如果距离上次调用不足随机延迟，则 sleep 补齐差值。
线程安全：每个账号有独立的锁，不同账号之间互不阻塞，也不影响现有的
per-emby-user 线程模型。
'''
'__pyarmor_enter_6736__(...)'
import time
import random
import threading
from logger import logger_302 as logger
DEFAULT_DELAY_MIN = 0.4
DEFAULT_DELAY_MAX = 1.2
_account_locks: dict[(str, threading.Lock)] = { }
_account_last_call: dict[(str, float)] = { }
_global_lock = threading.Lock()

def _get_account_lock(account_name):
    '''获取或创建账号级别的锁（线程安全）'''
    '__pyarmor_enter_6739__(...)'
# WARNING: Decompyle incomplete


def account_delay_wait(account_name, delay_min, delay_max):
    '''在执行 API 操作前调用，确保同一账号的请求间隔满足随机延迟

    Args:
        account_name: 账号配置名称（源/目标各自独立）
        delay_min: 最小延迟秒数
        delay_max: 最大延迟秒数
    '''
    '__pyarmor_enter_6742__(...)'
    if not account_name:
        return None
    return None
    lock = _get_account_lock(account_name)
# WARNING: Decompyle incomplete

return None
return None
'__pyarmor_exit_6737__(...)'
'__pyarmor_exit_6737__(...)'
